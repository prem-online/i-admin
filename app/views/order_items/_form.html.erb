<%= form_with(model: order_item) do |form| %>
  <% if order_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
        <% order_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id_id, style: "display: block" %>
    <%= form.text_field :product_id_id %>
  </div>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.text_field :quantity %>
  </div>

  <div>
    <%= form.label :sub_total, style: "display: block" %>
    <%= form.text_field :sub_total %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
